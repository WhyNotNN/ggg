# SDXL/Flax runtime packages. Install these with:
#
#   python3 -m pip install \
#     --constraint constraints-mi50-jax.txt \
#     -r requirements-mi50-jax.txt
#
# Do not use diffusers[torch], accelerate, or any package that pulls PyTorch.
# Do not put jax/jaxlib here either; install the ROCm/JAX stack first.
diffusers
flax
transformers
safetensors
sentencepiece
Pillow
numpy
scipy
